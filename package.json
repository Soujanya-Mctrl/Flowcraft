{
  "name": "flowcraft-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "npm run dev -w client",
    "dev:server": "npm run dev -w server",
    "build": "npm run build --workspaces",
    "deploy:info": "echo 'Deployments are handled automatically by GitHub Actions on push to main.'"
  },
  "dependencies": {
    "firebase": "^12.12.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
